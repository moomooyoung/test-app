trigger:
- main

pool:
  vmImage: 'ubuntu-latest'

variables:
  dockerHubServiceConnection: 'docker'
  acrServiceConnection: 'acr'
  dockerImageName: 'mooyoungacr.azurecr.io/test-app'
  azureSubscription: 'subscription'
  azureResourceGroup: 'DEV-AKS-RG'
  kubernetesCluster: 'dev-ci-cd-aks'
  helmChartPath: 'chart'
  releaseName: 'test-app'

steps:
- task: Docker@2
  displayName: Build and push Docker image
  inputs:
    command: buildAndPush
    repository: $(dockerImageName)
    dockerfile: '**/Dockerfile'
    containerRegistry: $(acrServiceConnection)
    tags: 'latest'   # 매 빌드마다 latest로 덮어쓰기
    arguments: '--no-cache'   # 추가

- task: HelmInstaller@1
  displayName: Install Helm
  inputs:
    helmVersionToInstall: 'latest'

- task: HelmDeploy@0
  displayName: Deploy to AKS
  inputs:
    connectionType: 'Azure Resource Manager'
    azureSubscription: '$(azureSubscription)'
    azureResourceGroup: '$(azureResourceGroup)'
    kubernetesCluster: '$(kubernetesCluster)'
    command: 'upgrade'
    install: true
    chartType: 'FilePath'
    chartPath: '$(helmChartPath)'
    releaseName: '$(releaseName)'
    overrideValues: |
      image.repository=$(dockerImageName)
      image.tag=latest
      imagePullSecrets[0].name=acr-secret

# AKS credentials 가져오기
- task: AzureCLI@2
  displayName: 'Get AKS credentials'
  inputs:
    azureSubscription: '$(azureSubscription)'
    scriptType: bash
    scriptLocation: inlineScript
    inlineScript: |
      az aks get-credentials --resource-group $(azureResourceGroup) --name $(kubernetesCluster) --overwrite-existing

# rollout restart
- script: |
    set -x
    kubectl rollout restart deployment $(releaseName) -n default
    kubectl get pods -n default
  displayName: 'Force restart deployment to apply latest image'
